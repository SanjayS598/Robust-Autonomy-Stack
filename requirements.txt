# Core dependencies for Robust Autonomy Stack
# IMPORTANT: Requires Python >=3.6,<3.12 (tested with Python 3.11)
# Install in conda environment: conda create -n metadrive python=3.11 -y && conda activate metadrive

# Core dependencies
numpy==1.24.2
scipy>=1.10.0
opencv-python>=4.8.0
shapely>=2.0.0
pyyaml==6.0.3
pydantic>=2.0.0

# Data and ML
matplotlib>=3.7.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# RL and Simulation
# Note: Version pinning is critical for compatibility
metadrive-simulator==0.4.2.2
gymnasium==0.28.1  # MetaDrive requires <0.29
stable-baselines3==2.2.1  # Compatible with gymnasium 0.28.1
torch>=2.3.0

