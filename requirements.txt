# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
opencv-python>=4.8.0
shapely>=2.0.0
pyyaml>=6.0
pydantic>=2.0.0

# Data and ML
matplotlib>=3.7.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# RL
stable-baselines3>=2.0.0
gymnasium>=0.29.0

# CARLA Python API
# Note: CARLA Python API has platform-specific native libraries (.so files)
# that don't work on macOS. We'll run the Python client in Docker alongside
# the CARLA server. See docker/compose.yml for the client container setup.
